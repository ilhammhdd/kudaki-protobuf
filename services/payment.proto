syntax = "proto3";
package service;

import "commands/user.proto";
import "events/user.proto";

option go_package = "";

service Payment {
  rpc MakePayment(command.MakePayment) returns (event.PaymentDone);
}

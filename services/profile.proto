syntax = "proto3";
package service;

import "commands/user.proto";
import "queries/user.proto";
import "events/user.proto";

option go_package = "";

service Profile {
  rpc UpdateProfile(command.UpdateProfile) returns (event.ProfileUpdated);
  rpc RetrieveProfile(query.RetrieveProfile) returns (event.ProfileRetrieved);
}